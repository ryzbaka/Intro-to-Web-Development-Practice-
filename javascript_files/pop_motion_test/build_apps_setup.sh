#Tool that helps build javascript application altogether
npm install --global parcel-bundler

#The tool that we are using is called Parcel
parcel --help

#Next we want to turn the folder into an npm project so we'll type
npm init -y # to initialize our project folder

#this will add a package.json file which is used for configuration
npm install popmotion

npm info somePackage # to see info about a library

parcel popmotion.html #build the npm project and run it on 
#a port
